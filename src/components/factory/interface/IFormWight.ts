import { FormEnum } from '@/components/factory/enum/FormEnum';
import IComponents from '@/interface/components/IComponents';
export default interface IFormWight {
    type?: FormEnum;
    param?: IComponents;
}